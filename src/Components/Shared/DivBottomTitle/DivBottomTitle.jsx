


const DivBottomTitle = ({subTitle, Title}) => {
    return (
        <div>
            <p>{subTitle}</p>
            <h1 className="txt-theme font-bold text-2xl">{Title}</h1>
          </div>
    );
};

export default DivBottomTitle;